<project xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns="http://maven.apache.org/POM/4.0.0">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <artifactId>{project.name}</artifactId>
        <groupId>{package}</groupId>
        <version>{pom.version}</version>
        <relativePath>../pom.xml</relativePath>
    </parent>

    <artifactId>{project.name}-infrastructure</artifactId>
    <name>{project.name}-infrastructure</name>
    <packaging>pom</packaging>
    <url>http://maven.apache.org</url>


    <modules>
        <module>{project.name}-infrastructure-producer</module>
        <module>{project.name}-infrastructure-repository</module>
        <module>{project.name}-infrastructure-rpc</module>
    </modules>
</project>
